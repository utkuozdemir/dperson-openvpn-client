---
name: build

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths-ignore:
      - "**.md"
      - ".gitignore"
  pull_request:
    branches:
      - master
    paths-ignore:
      - "**.md"
      - ".gitignore"
  # cron to run every sunday at 00:00 UTC
  schedule:
    - cron: "0 0 * * 0"

concurrency:
  group: build-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    env:
      CURRENT_BUILD: ghcr.io/utkuozdemir/dperson-pv-migrate:current-build
    runs-on: ubuntu-latest
    steps:
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          pull: true
          push: false
          tags: $CURRENT_BUILD

      - name: Extract alpine version
        run: |
          set -euo pipefail
          
          ALPINE_VERSION=$(docker run --rm $CURRENT_BUILD cat /etc/os-release | \
            grep VERSION_ID | \
            awk -F'=' ' { print $NF } ' | \
            tr -d '[:space:]')
          echo "Extracted ALPINE_VERSION=$ALPINE_VERSION"
          echo "ALPINE_VERSION=$(echo $ALPINE_VERSION)" >> $GITHUB_ENV

      - name: Extract openvpn version
        run: |
          set -euo pipefail
          
          OPENVPN_VERSION=$(docker run --rm $CURRENT_BUILD openvpn --version | \
            grep -o 'OpenVPN [0-9.]*' | \
            awk '{print $2}' | \
            tr -d '[:space:]')
          echo "Extracted OPENVPN_VERSION=$OPENVPN_VERSION"
          echo "OPENVPN_VERSION=$(echo $OPENVPN_VERSION)" >> $GITHUB_ENV

      - name: Re-tag the image with alpine and openvpn versions
        run: |
          set -euo pipefail
          
          # alpine and openvpn versions
          docker tag $CURRENT_BUILD ghcr.io/utkuozdemir/dperson-pv-migrate:alpine-$ALPINE_VERSION-openvpn-$OPENVPN_VERSION
          
          # only openvpn version
          docker tag $CURRENT_BUILD ghcr.io/utkuozdemir/dperson-pv-migrate:$OPENVPN_VERSION
          
          # latest
          docker tag $CURRENT_BUILD ghcr.io/utkuozdemir/dperson-pv-migrate:latest

      - name: Push the image to ghcr.io if this is not a pull request
        if: ${{ github.event_name != 'pull_request' }}
        uses: docker/build-push-action@v5
        with:
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          tags: |
            ghcr.io/utkuozdemir/dperson-pv-migrate:alpine-$ALPINE_VERSION-openvpn-$OPENVPN_VERSION
            ghcr.io/utkuozdemir/dperson-pv-migrate:latest
